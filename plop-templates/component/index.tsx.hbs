import React from 'react';
import { View, StyleSheet{{#if eq type 'atom'}}, ViewStyle{{/if}} } from 'react-native';
{{#if eq type 'atom'}}import { Text } from '../';{{else}}import { Text } from '@/components/atoms';{{/if}}
import { PRIMARY_COLOR, SECONDARY_TEXT } from '@/constants';

{{#if withProps}}
interface {{pascalCase name}}Props {
  title?: string;
  subtitle?: string;
  onPress?: () => void;
  disabled?: boolean;
{{#if eq type 'atom'}}  style?: ViewStyle;{{/if}}
  // TODO: Add more props as needed
}

export function {{pascalCase name}}({
  title = 'Default Title',
  subtitle,
  onPress,
  disabled = false,
{{#if eq type 'atom'}}  style,{{/if}}
}: {{pascalCase name}}Props) {
{{else}}
export function {{pascalCase name}}() {
{{/if}}
  return (
    <View style={[styles.container{{#if eq type 'atom'}}, style{{/if}}]}>
      <Text variant="h4" weight="semibold" color="primary">
        {{#if withProps}}{title}{{else}}{{pascalCase name}}{{/if}}
      </Text>
      
      {{#if withProps}}
      {subtitle && (
        <Text variant="body" color="secondary" style={styles.subtitle}>
          {subtitle}
        </Text>
      )}
      {{else}}
      <Text variant="body" color="secondary" style={styles.subtitle}>
        This is the {{pascalCase name}} component
      </Text>
      {{/if}}

      {/* TODO: Add your component content here */}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    padding: 16,
    borderRadius: 8,
    backgroundColor: 'transparent',
  },
  subtitle: {
    marginTop: 4,
  },
  // TODO: Add more styles as needed
});